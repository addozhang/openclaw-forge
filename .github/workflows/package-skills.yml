name: Package and Release Skills

on:
  push:
    tags:
      - 'v*'  # 触发条件：推送版本标签 (例如 v1.0.0)
  workflow_dispatch:  # 允许手动触发

jobs:
  package-and-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      
      - name: Install OpenClaw skill-creator
        run: |
          # 下载 OpenClaw skill-creator 打包脚本
          # 假设 package_skill.py 在 OpenClaw 仓库中
          curl -o package_skill.py https://raw.githubusercontent.com/openclaw/openclaw/main/skills/skill-creator/scripts/package_skill.py
          chmod +x package_skill.py
      
      - name: Package all skills
        run: |
          mkdir -p dist
          for skill_dir in skills/*/; do
            if [ -f "${skill_dir}SKILL.md" ]; then
              echo "Packaging ${skill_dir}..."
              python3 package_skill.py "${skill_dir}" dist/
            fi
          done
      
      - name: List packaged skills
        run: ls -lh dist/
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/*.skill
          body: |
            ## 📦 OpenClaw Skills Release
            
            打包的技能文件：
            
            ### Skills
            - **google-tasks** - Google Tasks API 集成
            - **research-prep** - 技术写作研究素材收集
            - **task-status** - 长时任务状态更新
            
            ### 安装方法
            
            下载 `.skill` 文件后，在 OpenClaw 中使用：
            
            ```bash
            openclaw skills install <skill-name>.skill
            ```
            
            或者手动解压到 workspace 的 `skills/` 目录。
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
